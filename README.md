# Group Whisper Transcript by Speaker

This Python script processes a Whisper `.json` transcript and groups the speech segments by speaker into natural blocks. 
It exports the grouped transcript to a `.docx` Word file with timestamps and speaker labels.
